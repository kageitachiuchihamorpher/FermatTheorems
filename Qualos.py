#The following are the global variables.
PIE = 3.1415; c = (3 * ((10)^8)); L = 6;

#The following are the scope variables.
x = 1000000000; y = 2; z = 300; i = (1/2);

def Qualos(i):
    for i in range(16):
        #This print statement serves a summation list with each providing what the total inertia (approximately) of the universe was during that 
        print(str("Inertia Accumulator: ") + str(int(x * ((-i)^10)) + (y)^(2) + z));
        #This return statment provides a guage for what the minimum inertia should be. It allows serves a mechanism to upholding online winnings.
        return((x*(-10)) + y + (z/4));
Qualos(i);